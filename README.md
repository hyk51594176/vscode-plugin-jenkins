# jenkins

## Configuration

```json
{
  "jenkins.username": "",
  "jenkins.password": "",
  "jenkins.url":
}
```

## Development

* Open this example in VS Code
* `npm install`
* `npm run compile`
* `F5` to start debugging
